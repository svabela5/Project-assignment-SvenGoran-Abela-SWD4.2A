The following libraires are needed for this program to work:
tkinter => Pre installed with python
wikipedia => can be installed using the following command: pip install wikipedia
nltk => can be installed using the following command: pip install nltk
vader sentiment => can be installed using the following command: pip install vaderSentiment
sklearn => can be installed using the following command: pip install sklearn
re => pre-installed with python